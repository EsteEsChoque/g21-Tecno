import React from 'react';

const Diagrama = ({ cantidad }) => {
  return (
    <div>
      <h3>Diagrama basado en la cantidad: {cantidad}</h3>
      {/* Aquí puedes renderizar algo relacionado con la cantidad */}
    </div>
  );
};

export default Diagrama;
